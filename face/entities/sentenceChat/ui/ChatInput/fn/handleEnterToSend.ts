// import { KeyboardEvent } from 'react'

/*export function handleEnterToSend(event: KeyboardEvent<HTMLTextAreaElement>, onSend: () => void) {
	if (event.key === 'Enter' && !event.shiftKey) {
		event.preventDefault()
		onSend()
	}
}*/
