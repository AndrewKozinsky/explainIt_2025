query Auth_getMe {
	auth_getMe {
		id
		email
		isUserConfirmed
		currentSubscription {
			tariffId
			tariffCode
			tariffName
			pricePaid
			balance
			includedFileStorageMb
			startsAt
			endsAt
		}
	}
}
