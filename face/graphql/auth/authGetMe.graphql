query Auth_getMe {
	auth_getMe {
		id
		email
		isUserConfirmed
		balance
		currentSubscription {
			tariffId
			tariffCode
			tariffName
			pricePaid
			balance
			includedFileStorageMb
			startsAt
			endsAt
		}
	}
}
