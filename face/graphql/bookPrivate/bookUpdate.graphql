mutation Book_update($input: UpdatePrivateBookInput!) {
	book_update(input: $input) {
		id
		author
		name
		languageCode
		note
		userId
		coverUrl
		uploadUrl
		fileName
		fileS3Key
		isFileUploaded
		chapters {
			id
			bookId
			name
			header
			note
		}
	}
}
