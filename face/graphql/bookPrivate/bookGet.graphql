query Book_get($input: GetPrivateBookInput!) {
	book_get(input: $input) {
		id
		author
		name
		languageCode
		note
		userId
		freeToUse
		chapters {
			id
			bookId
			name
			header
			note
		}
	}
}
