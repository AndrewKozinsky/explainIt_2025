query Book_get($input: GetPrivateBookInput!) {
	book_get(input: $input) {
		id
		author
		name
		languageCode
		note
		userId
		freeToUse
		coverUrl
		fileName
		isFileUploaded
		chapters {
			id
			bookId
			name
			header
			note
		}
	}
}
