mutation Book_create($input: CreatePrivateBookInput!) {
	book_create(input: $input) {
		id
		author
		name
		languageCode
		note
		userId
		chapters {
			id
			bookId
			name
			header
			note
		}
	}
}
