mutation VideoPrivate_update($input: UpdatePrivateVideoInput!) {
	video_private_update(input: $input) {
		id
		name
		contentType
		originalContent
		processedContent
		userId
		uploadUrl
		fileSizeMb
	}
}
