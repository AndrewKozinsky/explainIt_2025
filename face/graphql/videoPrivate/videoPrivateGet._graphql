query VideoPrivate_get($input: GetPrivateVideoInput!) {
	video_private_get(input: $input) {
		id
		name
		originalContent
		processedContent
		userId
		fileName
		fileS3Key
		fileUrl
		isFileUploaded
		fileSizeMb
	}
}
