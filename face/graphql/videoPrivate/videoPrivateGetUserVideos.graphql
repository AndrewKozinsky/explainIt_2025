query VideoPrivate_getUserVideos {
	video_private_user_videos {
		id
		name
		year
		languageCode
		contentType
		originalContent
		userId
		fileName
		fileS3Key
		fileUrl
		isFileUploaded
		fileSizeMb
	}
}
