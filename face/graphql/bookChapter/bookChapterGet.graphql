query BookChapter_get($input: GetBookChapterInput!) {
	book_chapter_get(input: $input) {
		id
		name
		header
		note
		content
		sentences {
			id
			startOffset
			length
		}
		book {
			id
			name
			author
			note
			userId
		}
	}
}
