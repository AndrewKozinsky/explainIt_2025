query BookChapter_get($input: GetBookChapterInput!) {
	book_chapter_get(input: $input) {
		id
		name
		header
		note
		originalContent
		processedContent
		sentences {
			id
			startOffset
			length
		}
		book {
			id
			name
			languageCode
			author
			note
			userId
		}
	}
}
