mutation BookChapter_update($input: UpdateBookChapterInput!) {
	book_chapter_update(input: $input) {
		id
		name
		header
		originalContent
		processedContent
		note
		book {
			id
			name
			languageCode
			author
			note
			userId
		}
	}
}
