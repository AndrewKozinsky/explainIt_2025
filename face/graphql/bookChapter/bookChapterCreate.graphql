mutation BookChapter_create($input: CreateBookChapterInput!) {
	book_chapter_create(input: $input) {
		id
		name
		header
		originalContent
		processedContent
		note
		book {
			id
			name
			languageCode
			author
			note
			userId
		}
	}
}
