// import { render, screen } from '@testing-library/react'
// import ArticleType from '../../../../../articlesData/articleType'
// import { LanguageLevelBadge } from '../LanguageLevelBadge'

/*describe('LanguageLevelBadge component', () => {
	it('should render component with right text', () => {
		render(<LanguageLevelBadge level={ArticleType.LangLevel.a2} />)

		expect(screen.getByTestId('lang-level-badge')).toBeInTheDocument()
		expect(screen.getByTestId('lang-level-badge__level').textContent).toBe(
			ArticleType.LangLevel.a2,
		)
		expect(screen.getByTestId('lang-level-badge__name').textContent).toBe(
			ArticleType.LangLevelName.a2,
		)
	})
})*/
