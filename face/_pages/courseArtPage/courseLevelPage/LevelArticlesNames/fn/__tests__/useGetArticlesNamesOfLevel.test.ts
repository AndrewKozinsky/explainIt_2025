// import { renderHook } from '@testing-library/react'
// import articleService from '../../../../../../articleService/articleService'
// import ArticleType from '../../../../../../articlesData/articleType'
// import { useGetArticlesNamesOfLevel } from '../useGetArticlesNamesOfLevel'

/*const articlesOfLevel = [
	{
		name: 'Name 1',
		url: 'url-1',
	},
	{
		name: 'Name 2',
		url: 'url-2',
	},
]*/

// jest.spyOn(articleService, 'getArticlesOfLevel').mockReturnValue(articlesOfLevel)

/*describe('useGetArticlesNamesOfLevel', () => {
	it('should return an array with 3 elements', () => {
		const hookWrapper = () => useGetArticlesNamesOfLevel(ArticleType.LangLevel.a1)

		const { result } = renderHook(hookWrapper)

		expect(result.current).toEqual(['Name 1', '*', 'Name 2'])
	})
})*/
