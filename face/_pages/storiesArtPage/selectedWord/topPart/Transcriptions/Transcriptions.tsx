// import React from 'react'
// import { useGetSelectedWord } from '../../common'
// import './Transcriptions.scss'

/*function Transcriptions() {
	const selectedWord = useGetSelectedWord()

	if (!selectedWord || !selectedWord.word.usTranscription || !selectedWord.word.enTranscription) {
		return null
	}

	return (
		<div className="transcriptions">
			<Transcription lang="en" transcription={selectedWord.word.enTranscription} />
			<Transcription lang="us" transcription={selectedWord.word.usTranscription} />
		</div>
	)
}*/

// export default Transcriptions

/*type TranscriptionProps = {
	lang: string
	transcription: string
}*/

/*function Transcription(props: TranscriptionProps) {
	const { lang, transcription } = props

	return (
		<div className="transcription">
			<span className="transcription__lang">{lang}</span>
			<span className="transcription__text">{transcription}</span>
		</div>
	)
}*/
