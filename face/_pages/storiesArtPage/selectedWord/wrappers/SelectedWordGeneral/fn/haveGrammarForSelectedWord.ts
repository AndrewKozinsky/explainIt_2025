// import { useMemo } from 'react'
// import { useGetSelectedWord } from '../../../common'

/*export function useHaveGrammarForSelectedWord(): boolean {
	const selectedWord = useGetSelectedWord()

	return useMemo(
		function () {
			if (!selectedWord || !selectedWord.phraseIds.length) {
				return false
			}

			return !!selectedWord.phraseIds.length
		},
		[selectedWord],
	)
}*/
