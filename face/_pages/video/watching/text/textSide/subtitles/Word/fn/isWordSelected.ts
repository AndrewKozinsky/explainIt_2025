// import { useWatchingStore } from '_pages/video/watching/watchingStore'

/**
 * Возвращает булево значение является ли это слово выделенным
 * @param thisSentenceId — идентификатор этого предложения
 * @param thisWordId — идентификатор этого слова
 */
/*export function useGetIsWordSelected(thisSentenceId: number, thisWordId: number): boolean {
	const selectedSentenceId = useWatchingStore((state) => state.populatedPlainText.selected.sentenceId)
	const selectedWordsIds = useWatchingStore((state) => state.populatedPlainText.selected.wordIds)

	return selectedSentenceId === thisSentenceId && selectedWordsIds.includes(thisWordId)
}*/
