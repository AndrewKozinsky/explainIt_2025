// import React, { ReactNode } from 'react'
// import './TilesWrapper.scss'

/*type TilesWrapperProps = {
	children: ReactNode | ReactNode[]
}*/

// Контейнер для списка ссылок-карточек на статьи
/*export function TilesWrapper(props: TilesWrapperProps) {
	const { children } = props

	return (
		<div className="tiles-wrapper" data-testid="tiles-wrapper">
			{children}
		</div>
	)
}*/
