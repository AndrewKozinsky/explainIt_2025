// import ContentLinkWrapper from '../ContentLinkWrapper/ContentLinkWrapper'
// import './MediaLink.scss'

/*type MediaLinkProps = {
	href: string
	info?: null | string
	name?: null | string
	onClick: () => void
	linkStatus: 'idle' | 'current' | 'nested'
}*/

/*function MediaLink(props: MediaLinkProps) {
	const { href, info, name, onClick, linkStatus } = props

	return (
		<ContentLinkWrapper href={href} status={linkStatus} onClick={onClick}>
			<div className='media-link'>
				{info && <p className='media-link__info'>{info}</p>}
				<p className='media-link__name'>{name}</p>
			</div>
		</ContentLinkWrapper>
	)
}*/

// export default MediaLink
