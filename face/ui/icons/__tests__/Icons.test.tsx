// import { render } from '@testing-library/react'
// import { LeftArrow, RightArrow } from '../ArrowIcon/ArrowIcon'
// import { CloseIcon } from '../CloseIcon/CloseIcon'
// import { ModalIcon } from '../ModalIcon/ModalIcon'

/*describe('Icons', () => {
	it('should render all icons', () => {
		const icons = [<LeftArrow />, <RightArrow />, <CloseIcon />, <ModalIcon />]

		for (let i = 0; i < icons.length; i++) {
			const { container } = render(icons[i])
			const $svgElem = container.children[0]

			expect($svgElem).toBeInTheDocument()
		}
	})
})*/
