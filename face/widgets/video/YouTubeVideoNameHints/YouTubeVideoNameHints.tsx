// import React from 'react'
// import Button from '@/shared/ui/formRelated/buttons/Button/Button'
// import './YouTubeVideoNameHints.scss'

/*function YouTubeVideoNameHints(props: { onSelect: (text: string) => void }) {
	function handleClick(text: string) {
		props.onSelect(text)
	}

	return (
		<div className='you-tube-video-name-hints'>
			<HintButton onClick={() => handleClick('How I Practice English Every Day')}>
				How I Practice English Every Day
			</HintButton>
			<HintButton onClick={() => handleClick('First Impressions of MOSCOW')}>
				First Impressions of MOSCOW
			</HintButton>
			<HintButton onClick={() => handleClick('How I Got My American Accent')}>
				How I Got My American Accent
			</HintButton>
			<HintButton onClick={() => handleClick('Daily Life English')}>Daily Life English</HintButton>
		</div>
	)
}*/

// export default YouTubeVideoNameHints

/*function HintButton(props: { children: string; onClick: () => void }) {
	return (
		<Button theme='outline' size='small' onClick={props.onClick}>
			{props.children}
		</Button>
	)
}*/
