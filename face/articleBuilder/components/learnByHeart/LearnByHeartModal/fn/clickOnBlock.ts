// import { useLearnByHeartStore } from '../../store/store'

/**
 * Возвращает функцию, открывающую или закрывающую модальное окно просмотра слов следующего урока
 * @open — должно ли модальное окно открываться или закрываться
 */
/*export function useGetOpenCloseModal(open: boolean) {
	return function () {
		useLearnByHeartStore.setState({ isModalOpen: open })
	}
}*/
