export const defUserEmail = 'admin@email.com'
export const defUserPassword = 'adminPassword'
// User will get this amount on balance if he confirms his personality with OAuth
export const welcomeBonus = 10

/*export async function isUserExists(userQueryRepository: UserQueryRepository, userId: number) {
	const createdUser = await userQueryRepository.getUserById(userId)
	expect(createdUser).toBeTruthy()
}*/
